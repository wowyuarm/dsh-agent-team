{
  "name": "@deepseek-ai/dsh-agent-team-bundle",
  "version": "0.1.0",
  "description": "Opt-in Agent Team collaboration bundle for DeepSeek Harness",
  "private": false,
  "type": "module",
  "files": [
    "cordis.patch.yml",
    "agent-presets/team-member/agent.cordis.yml",
    "packages/*/lib/**/*",
    "packages/*/package.json",
    "README.md",
    "README.zh.md"
  ],
  "scripts": {
    "typecheck": "tsc -p packages/agent-team/tsconfig.json && tsc -p packages/command-agent-team/tsconfig.json && tsc -p packages/tool-agent-team/tsconfig.json",
    "build": "tsc -p packages/agent-team/tsconfig.build.json && tsc -p packages/command-agent-team/tsconfig.build.json && tsc -p packages/tool-agent-team/tsconfig.build.json",
    "prepack": "npm run build",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "dependencies": {
    "@deepseek-ai/dsh-agent-team": "workspace:^",
    "@deepseek-ai/dsh-command-agent-team": "workspace:^",
    "@deepseek-ai/dsh-tool-agent-team": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "vite-tsconfig-paths": "^5.0.0",
    "vitest": "^4.1.8"
  }
}
